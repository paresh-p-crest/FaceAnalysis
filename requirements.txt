# Python dependencies for MyFace backend
# Install (everything, incl. face parsing): pip install -r requirements.txt
#   or: uv pip install --python .venv/Scripts/python.exe -r backend/requirements.txt

# --- Web / API ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
python-multipart>=0.0.9

# --- Computer Vision & ML ---
mediapipe==0.10.14
opencv-python-headless>=4.10.0
numpy>=1.26.0
Pillow>=10.0.0

# --- OpenAI ---
openai>=1.50.0

# --- PDF generation ---
reportlab>=4.2.0

# --- HTTP client (used by openai SDK) ---
httpx>=0.27.0

# --- Env files (local dev) ---
python-dotenv>=1.0.0

# --- PostgreSQL (async) ---
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0

# --- Media storage: Replit Object Storage backend (MEDIA_STORAGE_BACKEND=replit) ---
# The SDK only ships wheels for Python <3.13 (Replit runs 3.11). The marker skips
# it on 3.13+, where only the local filesystem backend is usable, so this file
# installs cleanly everywhere.
replit-object-storage>=1.0.2; python_version < "3.13"

# --- Face parsing (SegFormer via HuggingFace) — used by the `parsing` pipeline stage ---
# Heavy (pulls torch). Optional at runtime: if these are absent, face parsing is
# disabled gracefully (see backend/face_parsing.py `face_parsing_enabled`).
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.40.0
accelerate>=0.30.0
